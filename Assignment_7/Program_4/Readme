This is simple project which creats different processes internally as process-2,process-3.Parent process should wait till both the processes terminates.This project use make commands to build and execute.Use <make> or <make build> commad to build the project.Use <make help> for help and more details about how to give commands and execute the project. 
